{
  "name": "browser-relay-extension",
  "version": "0.1.4",
  "description": "Chrome extension for Browser Relay",
  "private": true,
  "scripts": {
    "build": "echo 'Extension build complete - no compilation needed'",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/chrome": "^0.0.268",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
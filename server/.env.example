# Console Relay Configuration

# Server port (default: 8765)
PORT=8765

# Enable MCP server mode for AI assistants
MCP_MODE=true

# Allowed domains for console log capture (comma-separated)
# If not set, logs will be captured from all websites
# Examples:
# CONSOLE_RELAY_ALLOWED_DOMAINS=localhost,example.com,myapp.local
# CONSOLE_RELAY_ALLOWED_DOMAINS=github.com,stackoverflow.com
CONSOLE_RELAY_ALLOWED_DOMAINS=